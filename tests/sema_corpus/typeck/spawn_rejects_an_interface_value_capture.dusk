@paradigm oop
interface I { get() -> int64 }
struct S { x: int64 }
impl I for S { func get() -> int64 { return self.x } }
func f() -> void {
  s := S { x: 1 }
  i: I = s
  t, e := spawn(lambda () -> void {
    println(i.get())
  })
  e.ignore()
  je := join(t)
  je.ignore()
}
