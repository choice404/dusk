struct Box<T> { x: T }
func main() -> int32 {
  b: Box<int64[]> = Box { x: [1, 2] }
  t, e := spawn(lambda () -> void {
    println(b.x.len)
  })
  e.ignore()
  je := join(t)
  je.ignore()
  return 0
}
