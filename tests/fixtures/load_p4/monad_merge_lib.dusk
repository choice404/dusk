@paradigm functional

export struct Opt<T> {
    v: T,
}

monad Opt {
    export func bind<A, B>(m: Opt<A>, f: (A) -> Opt<B>) -> Opt<B> {
        return f(m.v)
    }
    func unit<A>(x: A) -> Opt<A> {
        return Opt { v: x }
    }
}
