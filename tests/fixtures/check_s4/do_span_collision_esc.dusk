@paradigm functional
@import std.functional.maybe

func chain(a: Maybe<int64>, b: Maybe<int64>) -> Maybe<int64> {
    do {
        x <- a
        y <- b
        return unit(x + y)
    }
}

func main() -> int64 { return 0 }
