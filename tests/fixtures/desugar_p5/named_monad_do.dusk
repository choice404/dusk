@paradigm functional

monad Box {
    export func bind(m: int64, f: (int64) -> int64) -> int64 {
        return f(m)
    }
    export func unit(x: int64) -> int64 {
        return x
    }
}

func sample() -> int64 {
    return do Box {
        x <- Box.unit(1)
        x + 2
    }
}
