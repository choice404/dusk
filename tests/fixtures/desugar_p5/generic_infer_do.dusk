@paradigm functional

monad Gen {
    export func bind<A, B>(m: A, f: (A) -> B) -> B {
        return f(m)
    }
    export func unit<A>(x: A) -> A {
        return x
    }
}

func sample() -> int64 {
    return do Gen {
        x <- Gen.unit(1)
        x
    }
}
