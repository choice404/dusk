@paradigm procedural

struct Cell { n: int64 }

func make() -> *Cell {
    c: *Cell = alloc(Cell { n: 0 })
    ref q := c
    (*q).n = 5
    return c
}

func main() -> int32 {
    c := make()
    println((*c).n)
    free(c)
    return 0
}
