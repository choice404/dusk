// A generic whose type parameter no call site pins. The surface pass wildcards the
// unknown result, so only monomorphization reports the gap, at the source line.
@paradigm procedural
@import std.io

func pick<T>() -> T { return pick() }

func main() -> int32 {
    x := pick()
    println(x)
    return 0
}
