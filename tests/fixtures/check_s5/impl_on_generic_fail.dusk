// An impl block on a generic type would be dropped and its calls miscompiled, so
// monomorphization rejects it until per-instance impl expansion lands.
@paradigm oop

struct Box<T> { v: T }

impl Box {
    func get() -> int64 { return 0 }
}

func main() -> int32 { return 0 }
