@paradigm functional

@import p1
@import q1

// bind and unit are the generic do protocol, and a do block reaches them by
// their bare names, so no package scope can tell two of them apart.
func main() -> int32 {
    r := do {
        a <- 7
        a * 3
    }
    println(r)
    return 0
}
