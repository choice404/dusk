@paradigm procedural

@import su

// The twin of depstdsib with the project's std.string import taken away. The
// dependency's sibling file reads the same either way.
func main() -> int32 {
    println(su.go())
    return 0
}
