@paradigm procedural

@import su2

// The package exports a trim of its own and no file of it ever imports std.string.
export func trim(s: string) -> string {
    return "DEP-trim"
}

export func go() -> string {
    return use_trim()
}
