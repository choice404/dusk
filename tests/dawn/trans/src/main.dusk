@paradigm procedural

@import mid

// leaf is in the build because mid required it, and this project never did, so the
// refusal names the manifest line to write before there is an import to write.
func main() -> int32 {
    println(mid_tag())
    println(leaf_tag())
    return 0
}
