@paradigm procedural

@import cnum

// The same C export named through the alias it was required under. A name a
// package cannot scope is still a name the alias reaches.
func main() -> int32 {
    println(cnum.cabi_tag())
    return 0
}
