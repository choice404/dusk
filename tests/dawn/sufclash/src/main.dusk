@paradigm procedural

@import json
@import json_1

// The alias is spelled like the suffix the loader gives json.dusk's private names,
// so the two would produce one symbol from two declarations.
func main() -> int32 {
    println(local_parse())
    println(parse())
    return 0
}
