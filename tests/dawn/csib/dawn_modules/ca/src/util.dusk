@paradigm procedural

@import mb

// This file's package exports a C foo of its own, and the package it imported
// exports a moving foo. The sibling's export is the one the bare name means.
export func use_foo() -> int64 {
    return foo()
}
