@paradigm procedural

@import ca
@import mb

// One file imports the package whose foo is a C export and the package whose foo moves
// under a package suffix. A fixed name does not win the bare spelling for being fixed.
func main() -> int32 {
    println(foo())
    return 0
}
