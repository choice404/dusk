@paradigm procedural

@import su

// This file never imported std.string; its dependency did. The std trim is in the
// program without this file having reached for it, so the bare name picks neither.
func main() -> int32 {
    println(trim("  x  "))
    return 0
}
