@paradigm procedural

@import d

// The project reads the package's exported wrapper, and the file scoped declaration
// behind it is the whole of the case.
func main() -> int32 {
    println(d_hash(3))
    return 0
}
