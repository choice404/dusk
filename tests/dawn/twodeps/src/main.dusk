@paradigm procedural

@import a
@import b

// Both dependencies export parse. Each name is scoped to the package that
// exported it, so the qualified forms name two different functions.
func main() -> int32 {
    println(a.parse())
    println(b.parse())
    return 0
}
