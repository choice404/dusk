@paradigm functional

@import pm

// The project declares no pair of its own and never writes a do block: the whole
// protocol lives inside the dependency, where nothing exported it.
func main() -> int32 {
    println(pm_run())
    return 0
}
