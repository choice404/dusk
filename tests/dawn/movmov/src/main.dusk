@paradigm procedural

@import p

// A package is one scope, so a name it exports twice is a duplicate definition inside it
// rather than anything the importing file could qualify its way out of.
func main() -> int32 {
    println(p.via_a())
    return 0
}
