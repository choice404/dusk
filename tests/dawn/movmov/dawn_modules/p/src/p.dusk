@paradigm procedural

@import b

// Both files export foo the ordinary way, so both take the one package suffix and the
// two declarations land on one name.
export func foo() -> int64 {
    return 1
}

export func via_a() -> int64 {
    return foo()
}
