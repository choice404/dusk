@paradigm procedural

@import p

// The project asks the package for nothing but its tag. The refusal belongs to the
// package, so it stands whether or not anything reaches for the name it spelled twice.
func main() -> int32 {
    println(p.tag())
    return 0
}
