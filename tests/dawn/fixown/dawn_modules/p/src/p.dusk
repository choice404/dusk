@paradigm procedural

@import b

// The foo this package hands to C, which keeps its spelling through every rename.
export "C" func foo() -> int64 {
    return 1
}

export func tag() -> int64 {
    return 7
}
