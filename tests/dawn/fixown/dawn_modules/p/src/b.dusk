@paradigm procedural

// The same name exported the ordinary way from the file beside it, which the package
// suffix would move. One package cannot hold both readings of one name.
export func foo() -> int64 {
    return 2
}
