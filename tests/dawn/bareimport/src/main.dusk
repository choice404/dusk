@paradigm procedural

@import maybe

// The import brings the package's exports into this file's scope, so the
// dependency's function answers to its bare name here.
func main() -> int32 {
    println(maybe_tag())
    return 0
}
