@paradigm procedural

@import greet

// The dependency exports this name too. Exports are global, so the merged module
// cannot hold both, whatever either of them does.
export func greet_line() -> string {
    return "mine"
}

func main() -> int32 {
    println("unreachable")
    return 0
}
