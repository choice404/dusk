@paradigm procedural

@import std.string
@import su

// This file imports std.string for itself and never spells trim bare, so the only
// question the program asks is which trim the dependency's sibling file meant.
func main() -> int32 {
    println(su.go())
    return 0
}
