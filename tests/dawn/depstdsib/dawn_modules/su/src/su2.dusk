@paradigm procedural

// A sibling file of the same package. The bare trim it calls is its own package's
// export, and the std trim the project imported is not in this file's scope.
export func use_trim() -> string {
    return trim("  x  ")
}
