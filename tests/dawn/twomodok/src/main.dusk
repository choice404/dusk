@paradigm procedural

@import helper
@import pkg1
@import util

// The import in this file says which util the qualified call means, so the prefix is
// answered here even though two of the program's files spelled it differently.
func main() -> int32 {
    println(helper_line())
    println(pkg1.pkg1_line())
    println(util.local_thing())
    return 0
}
