@paradigm procedural

@import mb
@import ca

// The same two packages in the other import order, so the refusal cannot be an artifact
// of which one was read first.
func main() -> int32 {
    println(foo())
    return 0
}
