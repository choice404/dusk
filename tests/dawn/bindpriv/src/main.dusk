@paradigm functional

@import helper

// The project declares no pair of its own and the pair the other file wrote is private
// to that file, so neither do block in this program has a bind to reach.
func main() -> int32 {
    r := do {
        a <- 5
        a * 2
    }
    println(r)
    println(helper_do())
    return 0
}
