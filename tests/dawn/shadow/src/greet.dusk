@paradigm procedural

// Never loaded: the manifest binds greet to the dependency, so the alias answers
// the import and this file is only reachable under another name.
export func greet_line() -> string {
    return "local"
}
