@paradigm procedural

// A sibling file of the same package, so its bare vec_len is the one its own package
// exports, whatever the project that required the package imported for itself.
export func d2_count() -> int64 {
    return vec_len(7)
}
