@paradigm procedural

@import d2

// The package's own vec_len, which is not the standard library's and means something
// else. No file of this package ever imports std.vector.
export func vec_len(n: int64) -> int64 {
    return 999
}

export func d_count() -> int64 {
    return d2_count()
}
