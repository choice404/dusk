@paradigm procedural

@import std.vector

// A package export spelled exactly like a standard library function, over the standard
// library's own type. Declaring it is fine, and this file never spells the name bare.
export func vec_len<T>(v: *Vector<T>) -> int64 {
    return 999
}
