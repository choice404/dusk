@paradigm procedural

@import ca
@import other

// The C export bare, the same one through its alias, and a second package's moving
// foo reached through the project file that imported it.
func main() -> int32 {
    println(foo())
    println(ca.foo())
    println(other_foo())
    return 0
}
