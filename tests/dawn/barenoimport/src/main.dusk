@paradigm procedural

@import helper

// helper imports maybe and this file does not, so the dependency's export is
// in scope there and not here. Naming it bare from here is refused.
func main() -> int32 {
    println(helper_line())
    println(maybe_tag())
    return 0
}
