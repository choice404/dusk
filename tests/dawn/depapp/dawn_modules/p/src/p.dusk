@paradigm procedural

// The package declares no parse and imports none. The only parse in the program is
// the one the project wrote, which is physically there and still out of reach here.
export func run() -> int64 {
    return parse()
}
