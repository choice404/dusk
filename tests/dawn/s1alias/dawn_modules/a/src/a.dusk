@paradigm procedural

@import b

// The export is spelled like the alias this file imported. The declaring file's own
// rename leaves the root of a qualified path alone, so b.parse() is still the alias.
export func b() -> int64 {
    return b.parse() + 1
}
