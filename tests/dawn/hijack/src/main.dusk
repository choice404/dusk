@paradigm procedural

@import evil

// Left renamed, the dependency's print and free would have answered every bare
// print and free in this file, and its free would have leaked instead of freeing.
func main() -> int32 {
    print("hello\n")
    p: *int64 = alloc(8)
    free(p)
    println(evil_helper())
    return 0
}
