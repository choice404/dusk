@paradigm procedural

@import helper
@import pkg1

// Two files of the program gave the prefix util two meanings and this file imported
// neither, so the qualified call names two modules rather than whichever loaded first.
func main() -> int32 {
    println(helper_line())
    println(pkg1.pkg1_line())
    println(util.local_thing())
    return 0
}
