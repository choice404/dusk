@paradigm procedural

@import greet.greet_line

// The leaf form binds the one symbol it names, so the package's other export is
// not in scope here and naming it bare is refused.
func main() -> int32 {
    println(greet_line())
    println(greet_other())
    return 0
}
