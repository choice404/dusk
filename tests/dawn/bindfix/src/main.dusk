@paradigm functional

@import p1
@import q1

// Two packages, each with a private pair of its own. What they are not is the collision
// two exported pairs earn, since nothing here is exported.
func main() -> int32 {
    println(p1_go())
    println(q1_go())
    return 0
}
