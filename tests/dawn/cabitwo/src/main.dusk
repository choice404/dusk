@paradigm procedural

@import one
@import two

// Two packages export one C symbol. That name reaches the linker as written, so
// no package scope can tell the two apart and the program cannot be built.
func main() -> int32 {
    println(cabi_tag())
    return 0
}
