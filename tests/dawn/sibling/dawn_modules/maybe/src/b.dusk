@paradigm procedural

// This file never imports a. A package is one scope, so a sibling file's export
// answers to its bare name here.
export func b_line() -> string {
    return "b over " + a_tag()
}
