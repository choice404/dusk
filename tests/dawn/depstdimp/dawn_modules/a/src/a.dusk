@paradigm procedural

@import std.string
@import b

// This file asked for std.string itself and for a package that exports a trim of its
// own, so both answers are in its own scope and the bare name picks neither.
export func a_use() -> string {
    return trim("  x  ")
}
