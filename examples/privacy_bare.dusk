// A bare unqualified call cannot reach an imported module's private name; only
// exported names cross a file boundary. This program does not compile.
@import privlib_a

func main() -> int32 {
    println(helper())
    return 0
}
