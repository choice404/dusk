// Compile-fail twin of casts: the primitive width names double as the cast
// builtins, so a function cannot take one; the call form would be ambiguous.
@paradigm procedural

func int32(x: int64) -> int64 {
    return x + 7
}

func main() -> int32 {
    println(int32(35))
    return 0
}
