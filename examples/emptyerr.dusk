// toString on the empty error is the empty string, not a null that would crash
// the C printers.
func main() -> int32 {
    e := error {}
    e.ignore()
    println(e.toString())
    println("after")
    return 0
}
