// Spec audit: the unsigned integer literal suffixes are reserved for the same
// reason the type names are, so `5u8` is rejected in the lexer.
@paradigm procedural

func main() -> int32 {
    x := 5u8
    println(x)
    return 0
}
