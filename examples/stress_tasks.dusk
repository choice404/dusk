// Stress: 1000 async tasks, ten in flight at a time across 100 batches. Each
// batch mints ten bare futures from a direct async call (the sanctioned
// unannotated `f := one()` form gofanin.dusk uses) before awaiting any of
// them, so ten tasks sit pending on the loop's ready queue at once; a single
// loop thread drains that queue FIFO, so every batch's sum is exact and the
// grand total is deterministic at 1000 every run.
//
// Note: a bare future from a direct async call may now be stored in a
// Vector<Future<T>>, passed as an argument, or held through a Future<T>
// annotation, which futurefan.dusk exercises. This golden keeps its named
// per-batch bindings because they spell the ten-in-flight batching directly:
// ten futures minted, then ten awaited, one batch at a time.
@paradigm procedural
@import std.async.future
@import std.async.loop

async func one() -> int64 {
    return 1
}

async func amain() -> int32 {
    mut sum: int64 = 0
    mut batch := 0
    while batch < 100 {
        f0 := one()
        f1 := one()
        f2 := one()
        f3 := one()
        f4 := one()
        f5 := one()
        f6 := one()
        f7 := one()
        f8 := one()
        f9 := one()
        v0 := await f0
        v1 := await f1
        v2 := await f2
        v3 := await f3
        v4 := await f4
        v5 := await f5
        v6 := await f6
        v7 := await f7
        v8 := await f8
        v9 := await f9
        sum = sum + v0 + v1 + v2 + v3 + v4 + v5 + v6 + v7 + v8 + v9
        batch = batch + 1
    }
    println(sum)
    return 0
}

func main() -> int32 {
    le := loop_init()
    le.ignore()
    rc := async_run(amain())
    loop_free()
    return rc
}
