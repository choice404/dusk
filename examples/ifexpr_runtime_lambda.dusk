@paradigm procedural

// An if expression inside a lambda body yields the lambda call result.
func main() -> int32 {
    choose := lambda (c: bool, n: int64) -> int64 {
        return if c { n + 1 } else { n - 1 }
    }
    println(choose(true, 8))
    println(choose(false, 8))
    return 0
}
