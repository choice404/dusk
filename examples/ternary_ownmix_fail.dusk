@paradigm procedural

// Ternary branches must agree whether their pointer result is an owner.
func choose(c: bool, held: *int64) -> *int64 {
    value: *int64 = c ? held : alloc(2)
    return value
}

func main() -> int32 {
    return 0
}
