@paradigm procedural

// Two loop names are the map-pair form. A range yields one integer per iteration
// and has no key to bind, so binding two names over one rejects with the teach
// message rather than silently dropping the second.
func main() -> int32 {
    for k, v in 0..3 {
        println(k)
    }
    return 0
}
