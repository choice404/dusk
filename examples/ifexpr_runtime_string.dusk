@paradigm procedural

// A string branch tail selects as a normal if-expression value.
func main() -> int32 {
    c: bool = false
    message: string = if c { "left" } else { "right" }
    println(message)
    return 0
}
