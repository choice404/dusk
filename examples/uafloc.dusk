// A runtime fault names the file and line of the operation that faulted, not
// just a generic message, so a use after free points at its own deref line.
@paradigm procedural

func main() -> int32 {
    p: *int64 = alloc(9)
    println(*p)
    free(p)
    println(*p)
    return 0
}
