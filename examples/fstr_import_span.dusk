// The importer of fstr_import_span_helper: the pointer-in-hole error lives in the
// imported file, and the reported span must point there, proving the load merge
// shifts an f-string node's kids like any other expression.
@paradigm procedural

@import fstr_import_span_helper

func main() -> int64 {
    println("{}", bad_hole(1))
    return 0
}
