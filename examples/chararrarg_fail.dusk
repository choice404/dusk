// Compile-fail twin of chararrlit: the literal conversion lives at let and
// assignment only. A call argument position stays rejected.
@paradigm procedural

func f(a: char[2]) -> void {
    println(a)
}

func main() -> int32 {
    f("hi")
    return 0
}
