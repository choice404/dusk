@paradigm procedural

/** The glob cmd/*.dusk opens no comment, and this block never closes, so the fault is
 *  the doc block's own and points at the delimiter that opened it rather than at the
 *  glob inside it or at end of input.
func main() -> int32 {
    return 0
}
