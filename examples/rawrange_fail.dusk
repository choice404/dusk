// Compile-fail twin of strrange: a raw pointer has no length to validate a
// range against, so the range form is refused; index reads stay legal.
@paradigm procedural

func main() -> int32 {
    p: *raw char = alloc_bytes(4)
    sl: char[] = p[0..2]
    println(sl)
    free(p)
    return 0
}
