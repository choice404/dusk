// A local binding cannot take a builtin call name: a local closure named println is
// silently dead since the builtin wins, so the declaration is refused.
@paradigm procedural
func main() -> int32 {
    println := lambda (s: string) -> void { print(s) }
    println("hi\n")
    return 0
}
