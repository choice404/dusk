@paradigm procedural

func f() -> int64 {
    /** a doc block in a function body has no capture point, so it dangles */
    return 1
}

/** a doc block before an impl block dangles: an impl has no doc capture point */
impl Foo {
    func g() -> int64 {
        return 2
    }
}
