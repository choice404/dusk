@paradigm procedural

// owning on main is meaningless, nothing calls main to take its result, and
// rejecting it also pins the newline trap: a stray owning on its own line
// would otherwise silently attach to the next func head.
owning
func main() -> int64 {
    return 0
}
