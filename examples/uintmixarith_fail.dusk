// Signedness never mixes, the same word the width rule uses: a signed and an
// unsigned operand of the same width are still two different types, and the
// conversion between them is an explicit cast.
@paradigm procedural

func main() -> int32 {
    signed: int32 = 7
    unsigned: uint32 = 5
    println(signed + unsigned)
    return 0
}
