// civil_from_unix takes Unix seconds as an int64, not a string; there is no
// implicit parse, so this is a type mismatch at the call.
@paradigm procedural

@import std.time

func main() -> int32 {
    c := civil_from_unix("not a number")
    println(c.year)
    return 0
}
