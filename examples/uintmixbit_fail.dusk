// The bitwise and shift operators carry the signedness rule too. A right shift
// is the reason it matters: the signed form copies the sign bit down and the
// unsigned form shifts a zero in.
@paradigm procedural

func main() -> int32 {
    unsigned: uint32 = 12
    signed: int32 = 3
    println(unsigned & signed)
    return 0
}
