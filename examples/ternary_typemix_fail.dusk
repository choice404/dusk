@paradigm procedural

// Ternary branches use the same type-agreement rule as if-expression branches.
func main() -> int32 {
    c: bool = true
    small: int32 = 5
    big: int64 = 9
    value: int64 = c ? small : big
    println(value)
    return 0
}
