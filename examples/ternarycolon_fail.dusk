@paradigm procedural

// The conditional operator chooses between two branches, so a '?' with no ':' has no
// second branch to choose.
func main() -> int32 {
    c: bool = true
    n: int64 = c ? 1
    println(n)
    return 0
}
