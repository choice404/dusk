@paradigm procedural

// A ternary is an if expression and cannot yield a lambda literal branch.
func main() -> int32 {
    c: bool = true
    f := c ? lambda (n: int64) -> int64 { return n + 1 } : lambda (n: int64) -> int64 { return n - 1 }
    println(f(3))
    return 0
}
