@paradigm procedural

// A foreign block has no visibility of its own: its functions are the C symbols they
// name, so every importer reaches them by those names with nothing written in front.
export foreign "C" {
    func abs(x: int32) -> int32
}

func main() -> int32 {
    println(abs(-3))
    return 0
}
