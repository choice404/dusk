@paradigm procedural

// owning and async are mutually exclusive modifiers. Stacking them is a parse error:
// after 'async' the parser requires 'func', and 'owning' is not it.
async owning func f() -> int64 {
    return 1
}

func main() -> int32 {
    return 0
}
