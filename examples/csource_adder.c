/* A C source compiled into the program through the dusk @csource directive.
   The foreign block in csource_adder.dusk binds add against this definition. */
long add(long a, long b) {
    return a + b;
}
