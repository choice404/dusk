// Accept twin of stridxassign_fail: reading a string element is fine and
// prints its char as text; only an assignment target is refused.
@paradigm procedural

func main() -> int32 {
    s := "hello"
    c := s[0]
    println(c)
    return 0
}
