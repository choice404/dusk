// Strings have no ordering, so a relational operator between two strings is
// rejected; only == and != are legal.
@paradigm procedural

func main() -> int32 {
    a := "apple"
    if a < "y" {
        println("bad")
    }
    return 0
}
