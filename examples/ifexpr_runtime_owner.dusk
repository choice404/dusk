@paradigm procedural

// Fresh owners in both branches unify into an owning if-expression result.
func main() -> int32 {
    c: bool = true
    p: *int64 = if c { alloc(11) } else { alloc(22) }
    println(*p)
    free(p)
    return 0
}
