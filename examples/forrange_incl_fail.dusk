@paradigm procedural

// A counted loop takes the same half open range a slice bound does. The inclusive
// form is refused in the head rather than quietly counting one extra time, and the
// refusal names the rewrite.
func main() -> int32 {
    n: int64 = 3
    for i in 0..=n {
        println(i)
    }
    return 0
}
