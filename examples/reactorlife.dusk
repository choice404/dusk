// The reactor's lifecycle stands on its own: no event loop is needed to
// start it, print, and stop it again. Restartable and self contained.
@paradigm procedural
@import std.async.io

func main() -> int32 {
    se := reactor_start()
    se.ignore()
    println("ok")
    reactor_stop()
    return 0
}
