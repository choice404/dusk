@paradigm procedural

// A method call cannot suspend across the receiver's borrow, so an async
// method is rejected.
struct T {
    x: int64,
}

impl T {
    async func m() -> int64 {
        return 1
    }
}

func main() -> int32 {
    return 0
}
