@paradigm procedural

// An owning func hands over one owned value, not a tuple; a tuple return would leave
// the ownership of each element undeclared. Rejected at the declaration.
owning func f() -> (int64, int64) {
    return (1, 2)
}

func main() -> int32 {
    return 0
}
