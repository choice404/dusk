// An enum has no printable form, so a hole holding one is refused with the same
// diagnostic the format placeholder gives; match on it and print its parts.
@paradigm procedural

enum Color { Red, Green }

func main() -> int64 {
    c: Color = Color.Red
    s: string = f"c={c}"
    println("{}", s)
    return 0
}
