// comments_split_directive: a block comment splitting a directive word is the one
// documented benign divergence. prescan may reconstruct a directive across the gap
// for its scan dump, but the parser sees the split tokens and rejects it loudly, so
// a build never silently accepts it.
@paradigm procedural

@im/*x*/port foo

func main() -> int32 {
    return 0
}
