// An interpolated string cannot nest inside another's hole; bind the inner one to
// a name first.
@paradigm procedural

func main() -> int64 {
    s: string = f"a {f"b"} c"
    println("{}", s)
    return 0
}
