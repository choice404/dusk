@paradigm procedural

// A postfix increment in a loop condition yields before it updates the counter.
func main() -> int32 {
    mut i: int64 = 0
    n: int64 = 3
    mut count: int64 = 0
    while i++ < n {
        count++
    }
    println(count)
    println(i)
    return 0
}
