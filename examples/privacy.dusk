// Two imported modules may each keep a private helper of the same name; each
// exported function still reaches its own file's helper.
@import privlib_a
@import privlib_b

func main() -> int32 {
    println(from_a())
    println(from_b())
    return 0
}
