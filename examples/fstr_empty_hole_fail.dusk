// An empty hole has no expression to write; the diagnostic distinguishes it from
// the {} format placeholder that println uses.
@paradigm procedural

func main() -> int64 {
    s: string = f"a {} b"
    println("{}", s)
    return 0
}
