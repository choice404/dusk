// A write to the stderr writer flushes stdout first, the rule printerr follows, so the
// half line already printed reaches the terminal before the error line does.
@paradigm procedural

@import std.io

func main() -> int32 {
    print("a")
    e := write_line(stderr_writer(), "b")
    e.ignore()
    println("")
    return 0
}
