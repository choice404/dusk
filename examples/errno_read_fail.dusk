@paradigm procedural
@import std.os

// errstr takes exactly one argument, the errno code; a second is rejected at
// check time like any other arity mismatch.
func main() -> int32 {
    s := errstr(1, 2)
    println(s)
    return 0
}
