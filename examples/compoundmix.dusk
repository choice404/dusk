@paradigm procedural

// The place type governs a compound assignment's arithmetic; mixing widths on
// the right is the same error the plain binary form gives.
func main() -> int32 {
    mut x: int32 = 1
    y: int64 = 2
    x += y
    println(x)
    return 0
}
