@paradigm procedural

enum List<T> {
    Cons(head: T, tail: *List<T>),
    Nil,
}

func main() -> int32 {
    tail: *List<float64> = alloc(List.Nil)
    defer free(tail)
    node: List<float64> = List.Cons(2.5, tail)
    match node {
        Cons(h, t) => println(h),
        Nil => println(0),
    }
    return 0
}
