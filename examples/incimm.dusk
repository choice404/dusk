@paradigm procedural

// ++ desugars to a compound assignment, so it inherits the mut rule: an
// immutable binding cannot be incremented.
func main() -> int32 {
    x := 5
    x++
    println(x)
    return 0
}
