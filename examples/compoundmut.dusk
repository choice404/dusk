@paradigm procedural

// Compound assignment follows the plain assignment mut rule: an immutable
// binding cannot be the target of +=.
func main() -> int32 {
    x := 1
    x += 1
    println(x)
    return 0
}
