@paradigm procedural

// ++ has no value: it is a statement, not an expression, so it cannot appear
// on the right of a let binding.
func main() -> int32 {
    mut x: int64 = 5
    y := x++
    println(y)
    return 0
}
