// writef hands back the write error, so leaving the result of a call unbound is refused
// by the must handle rule the same way write_string is.
@paradigm procedural

@import std.fmt
@import std.io

func main() -> int64 {
    writef(stdout_writer(), "%d\n", [arg_int(1)])
    return 0
}
