@paradigm procedural

// An exported C function cannot be named dusk__main either: codegen reserves that
// name for the entry wrapper and gives it the collector anchor prologue, which a
// library entry point must never carry.
export "C" func dusk__main(x: int64) -> int64 {
    return x
}
