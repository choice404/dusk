// Compile-fail: the left side of an assignment must be a place. A range slice
// mints an rvalue view, so writing to one has no storage to land in.
@paradigm procedural

func main() -> int32 {
    mut xs: int64[4] = [1, 2, 3, 4]
    xs[0..2] = [7, 8]
    println(xs[0])
    return 0
}
