// comments_loader_live_twin: the compile-fail twin of comments_loader_dead_import.
// With the comment removed the @import is live, so the missing module is a load
// error.
@paradigm procedural

@import nosuch.mod

func main() -> int32 {
    println(42)
    return 0
}
