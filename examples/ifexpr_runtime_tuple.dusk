@paradigm procedural

// Both branches can yield the same tuple type into a destructuring binding.
func main() -> int32 {
    c: bool = true
    pair: (int64, string) = if c { (3, "yes") } else { (8, "no") }
    number, word := pair
    println(number)
    println(word)
    return 0
}
