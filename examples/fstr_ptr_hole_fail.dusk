// A managed pointer is not printable, so a hole holding one is refused; the fix is
// to dereference it or print its fields.
@paradigm procedural

func main() -> int64 {
    p: *int64 = alloc(5)
    s: string = f"p={p}"
    println("{}", s)
    free(p)
    return 0
}
