@paradigm procedural

// An async func's task frame and future are laid out at a single declared
// shape, so it cannot take type parameters.
async func g<T>(x: T) -> T {
    return x
}

func main() -> int32 {
    return 0
}
