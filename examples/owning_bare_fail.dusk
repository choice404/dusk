@paradigm procedural

// A lone owning must be followed by func: it is a func-head modifier, not a standalone
// item. Here it precedes a struct, which is a parse error.
owning struct S {
    x: int64,
}

func main() -> int32 {
    return 0
}
