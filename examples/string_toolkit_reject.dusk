// str_join takes a *Vector<string>, not a string. Passing a string literal where
// the vector of parts belongs is a wrong-type argument the checker rejects before
// any code runs.
@paradigm procedural

@import std.string

func main() -> int32 {
    println(str_join("a,b,c", ","))
    return 0
}
