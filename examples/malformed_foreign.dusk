// A malformed foreign block: a non-func keyword where a `func` declaration is
// expected. The parser must reject it in bounded time, not spin forever.
foreign "C" { struct x }
func main() -> int32 { return 0 }
