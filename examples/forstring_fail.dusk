// Compile-fail twin of forstring: a for iterand must have an element. An
// integer is rejected at check rather than dying in the linker.
@paradigm procedural

func main() -> int32 {
    for x in 5 {
        println(x)
    }
    return 0
}
