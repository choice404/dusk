// Importing a module that reserves a builtin name surfaces the reservation reject, not just
// the paradigm gate's usage error: the imported module's `func fold` is refused.
@paradigm procedural
@import collide_fold_mod
func main() -> int32 {
    println("{}", fold(1))
    return 0
}
