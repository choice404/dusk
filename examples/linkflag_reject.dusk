// A link value opening with '-' would reach clang as an option instead of a
// library or a path; the prescan rejects it before anything builds.
@paradigm procedural

@link "-Wl,-rpath,/tmp"

func main() -> int32 {
    return 0
}
