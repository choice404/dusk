// A string literal first argument is a format string at any arity, so doubled
// braces escape the same way with and without value arguments.
func main() -> int32 {
    println("{{}}")
    println("a {{b}} c")
    println("{{}} {}", 1)
    return 0
}
