// hello.cl, minimal program.
// Does not compile yet. The frontend lands at M1 onward. Kept as a syntax anchor
// and a future golden test input.

@paradigm procedural

@import std.io.println

func main(argc: int32, argv: string[]) -> int32 {
    println("hello, world")
    return 0
}
