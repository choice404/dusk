// Compile-fail twin of chararrlit: a destructuring binder's annotation must
// match the tuple member it binds; a char array cannot take a string member.
@paradigm procedural

func main() -> int32 {
    a: char[2], b: int64 := ("hi", 1)
    println(a)
    println(b)
    return 0
}
