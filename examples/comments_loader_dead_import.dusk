// comments_loader_dead_import: the loader loads imports from the AST, and a
// commented-out @import produces no token, so the missing module is never loaded
// and the program builds and runs.
@paradigm procedural

/* @import nosuch.mod */

func main() -> int32 {
    println(42)
    return 0
}
