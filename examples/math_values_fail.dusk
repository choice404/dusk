@paradigm procedural
@import std.math

// Every std.math binding takes a float64; a string argument is a type
// mismatch caught at check time, the same as calling any other function on
// the wrong argument type.
func main() -> int32 {
    x := sqrt("nope")
    println(x)
    return 0
}
