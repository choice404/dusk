// A bare closing brace in a chunk is a mistake, stricter than the format language;
// write '}}' for a literal brace.
@paradigm procedural

func main() -> int64 {
    s: string = f"a } b"
    println("{}", s)
    return 0
}
