// run_capture's cmd parameter is a string; a bare int literal argument is
// rejected at the call, not coerced or forwarded to the shell as text.
@paradigm procedural

@import std.process

func main() -> int32 {
    out, code, e := run_capture(42)
    e.ignore()
    println(out)
    println(code)
    return 0
}
