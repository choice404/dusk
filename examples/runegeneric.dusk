@paradigm procedural

func identity<T>(x: T) -> T {
    return x
}

func pair<T>(x: T, n: int64) -> (T, int64) {
    return (x, n)
}

func main() -> int32 {
    r := identity(r'\u{1F600}')
    println(r)

    a, n := pair(identity(r'中'), 12)
    println(a)
    println(n)
    return 0
}
