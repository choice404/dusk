@paradigm procedural

// An async func's return crosses back into a resumed caller frame that may
// no longer match the task's view, so a slice, closure, or interface value
// return is rejected.
async func g() -> int64[] {
    return []
}

func main() -> int32 {
    return 0
}
