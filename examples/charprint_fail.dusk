// Compile-fail twin of charprint: only a char element prints as text. An int
// array still has no printer, so opening the char arms must not open this.
@paradigm procedural

func main() -> int32 {
    a: int64[2] = [1, 2]
    println(a)
    return 0
}
