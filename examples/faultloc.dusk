// A runtime fault names the file and line of the operation that faulted, not
// just a generic message, so an out of bounds index points at its own line.
@paradigm procedural

func main() -> int32 {
    xs: int64[2] = [7, 8]
    println(xs[1])
    println(xs[9])
    return 0
}
