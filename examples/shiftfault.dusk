@paradigm procedural

// A dynamic shift amount out of [0, width) is a named runtime fault, not a
// silently masked shift.
func main() -> int32 {
    println(0)
    n: int64 = 64
    println(1 << n)
    return 0
}
