// A width cast only takes an integer-family value, so a float operand is
// rejected instead of silently truncating toward zero.
@paradigm procedural

func main() -> int32 {
    f: float64 = 3.5
    println(int32(f))
    return 0
}
