// Compile-fail: a struct-literal field literal must fit the declared field
// width, the same as a binding, argument, return, or enum payload literal.
@paradigm procedural

struct P {
    x: int32,
}

func main() -> int32 {
    p := P { x: 2147483648 }
    println(p.x)
    return 0
}
