// A range slice of a string validates its window against the NUL scanned
// length like every other base, so a window past the terminator faults
// instead of minting a slice over foreign bytes.
@paradigm procedural

func main() -> int32 {
    s := "abc"
    sl: char[] = s[1..9]
    println(sl)
    return 0
}
