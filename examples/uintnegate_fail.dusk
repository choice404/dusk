// Unary minus takes a signed integer or a float. Negating an unsigned value
// wraps to the top of its range instead of going below zero, which is almost
// always a width bug, so the cast has to say so out loud.
@paradigm procedural

func main() -> int32 {
    ticks: uint32 = 5
    println(-ticks)
    return 0
}
