// A void value cannot be printed, so a hole calling a void function is refused.
@paradigm procedural

func nothing() -> void {
    return
}

func main() -> int64 {
    s: string = f"x={nothing()}"
    println("{}", s)
    return 0
}
