@paradigm procedural

/** Builds every root under cmd/*.dusk and lib/*.dusk. A doc block does not nest, so the
 *  glob in that sentence opens no comment: the block ends at its own delimiter and the
 *  code below it is code. A plain block comment still nests. */
func main() -> int32 {
    println(7)
    return 0
}
