@paradigm procedural
@import std.async.future

// A future belongs to the event loop thread. It cannot cross an async func's
// signature as a parameter; the caller awaits it instead.
async func g(f: Future<int64>) -> void {
    return
}

func main() -> int32 {
    return 0
}
