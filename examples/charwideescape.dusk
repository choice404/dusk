@paradigm procedural

// A char holds one byte. A wide \u escape does not fit, so it is rejected with
// a hint to use a rune literal or a string.
func main() -> int32 {
    c: char = '\u{4E2D}'
    println(c)
    return 0
}
