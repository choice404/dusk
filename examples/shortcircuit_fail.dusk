// Both operands of a logical operator must already be bool, so an int on
// either side is rejected at the operator rather than truthiness-coerced.
@paradigm procedural

func main() -> int32 {
    if true && 5 {
        println("bad")
    }
    return 0
}
