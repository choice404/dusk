// A library with a private helper. Its name never leaves this file: the loader
// renames it before the merge, so another module's private helper of the same
// name cannot collide with it.
func helper() -> int64 {
    return 1
}

export func from_a() -> int64 {
    return helper()
}
