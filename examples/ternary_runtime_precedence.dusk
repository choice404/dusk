@paradigm procedural

// `||` binds before ternary, while assignment takes the whole selected value.
func main() -> int32 {
    x: bool = false
    y: bool = true
    println(x || y ? 1 : 2)
    mut result: int64 = 0
    result = false ? 3 : 4
    println(result)
    return 0
}
