@paradigm procedural

// ** needs both operands to be the same numeric type; an int base with a
// float exponent does not implicitly convert.
func main() -> int32 {
    x: int64 = 2
    println(x ** 2.0)
    return 0
}
