// A literal backslash directly before u{ stays six plain bytes; it is not a
// unicode escape, and the char array count and the printed text agree.
@paradigm procedural

func main() -> int32 {
    s: char[6] = "\\u{41}"
    println(s)
    t := "\\u{7a}"
    println(t)
    return 0
}
