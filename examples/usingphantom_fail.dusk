// Spec audit: a parameter typed with a name no type declares is rejected. A
// `using` parameter of a phantom type would allocate an unsized frame slot and
// abort in the backend, so it is caught here with a clear message.
@paradigm procedural

func work(using c: Collector) -> int32 {
    return 0
}

func main() -> int32 {
    return 0
}
