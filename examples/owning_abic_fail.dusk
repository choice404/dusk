@paradigm procedural

// An exported C function has a C calling convention and no dusk ownership; the owning
// marker never applies to it, so export "C" owning is rejected at the head.
export "C" owning func f() -> int64 {
    return 1
}

func main() -> int32 {
    return 0
}
