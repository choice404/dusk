// comments_scan_string_shield: a /* inside a directive's quoted string does not
// open a block comment, so the @import on the next line is still collected.
@paradigm procedural
@link "z/*q"
@import std.string
