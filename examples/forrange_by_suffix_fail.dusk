@paradigm procedural

// A suffixed literal is not the bare integer literal the step takes: the cursor
// counts in int64, so `by 2i32` names a width the step does not have rather than
// silently widening.
func main() -> int32 {
    for i in 0..6 by 2i32 {
        println(i)
    }
    return 0
}
