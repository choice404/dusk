// A malformed interface body: the method name is written with a stray `func`
// keyword, which correct dusk omits (`interface A { speak() -> int64 }`). The
// parser must reject it with a diagnostic in bounded time, not spin forever.
@paradigm oop
interface A { func speak() -> int64 }
func main() -> int32 { return 0 }
