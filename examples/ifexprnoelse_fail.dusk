@paradigm procedural

// An if used as a value needs both branches: with no else there is no value for the
// binding to take when the condition is false.
func main() -> int32 {
    c: bool = true
    n: int64 = if c { 1 }
    println(n)
    return 0
}
