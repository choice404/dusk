@paradigm procedural

// A double free. The block is already parked on the free list after the first
// free, so the second free faults instead of parking it twice.
func main() -> int32 {
    p: *int64 = alloc(5)
    println(*p)
    free(p)
    free(p)
    return 0
}
