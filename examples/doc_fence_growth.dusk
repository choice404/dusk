/** Shows a fence surviving inside a fence: the emitted fence must grow one
 * backtick past the longest run in the example, so this block renders intact.
 * @example
 * ```
 * x: int64 = 1
 * ```
 */
func fenced() -> void {
}
