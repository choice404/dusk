@paradigm procedural

struct Point { x: int64, y: int64 }

// A struct by value on an export is not supported in this line; the parameter
// must be a scalar, a *raw T, or a *void.
export "C" func lib_take(p: Point) -> int64 {
    return p.x
}
