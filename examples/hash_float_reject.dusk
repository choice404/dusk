@paradigm procedural

// A float is not a hashable key: NaN is never equal to itself, which breaks the
// hash and equality coherence a map needs.
func main() -> int32 {
    f: float64 = 2.5
    n: int64 = hash(f)
    return int32(n)
}
