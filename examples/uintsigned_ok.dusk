// Accept twin of the uint reservations: the signed widths and their suffixes are
// the supported spelling, so an int8 annotation and an i32 suffix both work.
@paradigm procedural

func main() -> int32 {
    x: int8 = 5
    println(x)
    y := 7i32
    println(y)
    return 0
}
