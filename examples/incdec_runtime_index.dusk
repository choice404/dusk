@paradigm procedural

// Postfix indexes before stepping; prefix steps before indexing.
func main() -> int32 {
    arr: int64[3] = [10, 20, 30]
    mut idx: int64 = 0
    println(arr[idx++])
    println(idx)
    println(arr[++idx])
    println(idx)
    return 0
}
