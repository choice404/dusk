@paradigm procedural
@import std.rand

// rng_next takes exactly the generator pointer; a second argument is an
// arity mismatch caught at check time.
func main() -> int32 {
    r: *Rng = rng_new(1)
    v := rng_next(r, 2)
    println(v)
    free(r)
    return 0
}
