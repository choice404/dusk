// Spec audit: the unsigned integer type names are reserved. They would lower to
// the same width as their signed twin and run every operation on the signed
// path, so a type that names one is refused up front.
@paradigm procedural

func main() -> int32 {
    x: uint8 = 5
    println(x)
    return 0
}
