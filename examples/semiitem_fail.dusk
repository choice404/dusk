@paradigm procedural

// The terminator belongs to a statement, and a module body holds declarations, so a
// semicolon after a top level declaration ends nothing.
func one() -> int64 {
    return 1
};

func main() -> int32 {
    println(one())
    return 0
}
