@paradigm procedural

// A step of zero never moves the cursor, so the loop would either spin forever or
// run zero times depending on which way the comparison fell. It is refused at the
// literal rather than compiled into either.
func main() -> int32 {
    for i in 0..3 by 0 {
        println(i)
    }
    return 0
}
