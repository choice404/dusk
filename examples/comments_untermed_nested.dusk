// comments_untermed_nested: nesting means one closing */ leaves the outer block
// open, so this still runs to end of file as an unterminated block comment.
@paradigm procedural

func main() -> int32 {
    println(0)
    return 0
}
/* outer /* inner */ still open
