@paradigm procedural

// An owning func hands over the one value it owns, so a void return is a contradiction:
// there is nothing to own. Rejected at the declaration.
owning func f() -> void {
    return
}

func main() -> int32 {
    return 0
}
