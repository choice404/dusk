@paradigm procedural

// An async func's frame outlives the call, so a parameter that may view the
// caller's frame, a slice, a closure, or an interface value, is rejected.
async func g(xs: int64[]) -> void {
    return
}

func main() -> int32 {
    return 0
}
