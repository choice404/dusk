// Compile-fail twin of strrange: a *void pointer has no length to validate a
// range against, the same refusal a raw pointer gets.
@paradigm procedural

func main() -> int32 {
    p: *void = alloc_bytes(4)
    sl := p[0..2]
    println(sl.len)
    free(p)
    return 0
}
