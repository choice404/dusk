@paradigm procedural

// hash is a builtin call form, so a top-level function cannot take its name; a
// call would otherwise resolve ambiguously between the builtin and the function.
func hash(v: int64) -> int64 {
    return 777
}

func main() -> int32 {
    return int32(hash(1))
}
