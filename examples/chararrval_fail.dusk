// Compile-fail twin of chararrlit: only a literal converts. A string typed
// value never initializes a char array, so the conversion cannot be laundered
// through a binding.
@paradigm procedural

func main() -> int32 {
    t := "x"
    s: char[1] = t
    println(s)
    return 0
}
