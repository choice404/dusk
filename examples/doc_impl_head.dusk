@paradigm procedural

interface Speaker {
    speak() -> int64
}

struct Dog { legs: int64 }

/** Dog speaks by barking; this documents the whole impl block, not any one method. */
impl Speaker for Dog {
    func speak() -> int64 {
        return 1
    }
}
