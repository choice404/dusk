// A format with no directive at all still needs an argument slice, and the empty slice
// checks and runs as one. %% writes a single percent and consumes nothing.
@paradigm procedural

@import std.fmt

func main() -> int64 {
    s: string = format("plain %%", [])
    println(s)
    free(s)
    return 0
}
